{
    "version": 1,
    "phases": {
      "install": {
        "commands": [
          "pip install --upgrade pip",
          "pip install -r requirements.txt"
        ]
      },
      "build": {
        "commands": []
      },
      "start": {
        "commands": [
          "uvicorn main:app --host 0.0.0.0 --port $PORT"
        ]
      }
    }
  }
  